name: Multi OS

on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:
  WinduntuOS:
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          echo "Hello from < nom de l'os >"
